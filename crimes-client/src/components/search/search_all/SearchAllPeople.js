import React, { Component } from 'react'
import { connect } from 'react-redux'

export class SearchAllPeople extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

const mapStateToProps = (state) => ({
    
})
export default connect(mapStateToProps, null)(SearchAllPeople)
